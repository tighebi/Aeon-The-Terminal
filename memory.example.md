# Example persistent context for chat

This is an example memory/context file.

Copy this to your real memory file if you want:

```zsh
mkdir -p ~/.config/chat
cp memory.example.md ~/.config/chat/memory.md